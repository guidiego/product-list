name: Test
on:
  pull_request:
  push:
    branches: master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install Packages
        run: yarn install

      - name: Lint
        run: yarn lint

      - name: Typecheck
        run: yarn tsc
